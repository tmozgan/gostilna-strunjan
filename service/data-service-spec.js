describe('dataService', function() {

  beforeEach(module('gostilnaassignment'));

  it('should ...', inject(function(dataService) {

    //expect(dataService.doSomething()).toEqual('something');

  }));

});
