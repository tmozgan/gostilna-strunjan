describe('emailService', function() {

  beforeEach(module('gostilnaassignment'));

  it('should ...', inject(function(emailService) {

    //expect(emailService.doSomething()).toEqual('something');

  }));

});
